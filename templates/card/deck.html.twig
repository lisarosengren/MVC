{# templates/card/deck.html.twig #}

{% extends "base.html.twig" %}

{% block title %}Sorterad kortlek{% endblock %}

{% block body %}
    <div class="card">
        <div class="deck">
            {% for div in deck %}
                {{div|striptags('<div>')|raw }}
            {% endfor %}
        </div>
    </div>
{% endblock %}
