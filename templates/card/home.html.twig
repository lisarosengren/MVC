{% extends "base.html.twig" %}

{% block title %}Kortlek{% endblock %}

{% block body %}

<h1>Kortlek</h1>

<p>Jag har skapat fyra klasser. Card, CardGraphic, CardHand och DeckOfCards. CardGraphic ärver från Card och GraphicCard och DeckOfCards är relaterade via komposition(aggregation), DeckOfCards skapar 52 objekt av GraphicCard. Aggregation är också relationen mellan Card och CardHand.</p>

<img src="{{ asset('build/images/kmom02mvc.png') }}" alt="UML kmom02" width=500>

<p>Här är sidorna som skapats i den här uppgiften:</p>

<ul>
    <li><a href="{{ path('card_start') }}">Den här sidan</li>
    <li><a href="{{ path('session') }}">Kontrollera session</li>
    <li><a href="{{ path('deck') }}">Titta på den sorterade kortleken</li>
    <li><a href="{{ path('shuffle') }}">Ta en ny kortlek, blanda och titta</li>
    <li><a href="{{ path('draw') }}">Dra ett kort</li>
    <li><a href="{{ path('draw_many', {num: 5}) }}">Dra flera kort</li>
</ul>


{% endblock %}